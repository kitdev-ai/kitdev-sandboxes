# Cloudflare DNS-01 credential indirection. Install as
# /etc/kitdev-sandboxes/ingress/acme-provider.env, root:root mode 0600.
# Use a scoped API token with Zone:DNS:Edit and Zone:Zone:Read for kitdev.ai.
CLOUDFLARE_DNS_API_TOKEN_FILE=/etc/kitdev-sandboxes/ingress/cloudflare-dns-api-token
