# Copy only the variables required by the selected lego DNS provider to
# /etc/kitdev-sandboxes/ingress/acme-provider.env as root:root mode 0600.
# Example for a provider using a token-file variable:
# PROVIDER_API_TOKEN_FILE=/etc/kitdev-sandboxes/ingress/provider-token
