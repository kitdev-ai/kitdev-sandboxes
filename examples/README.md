# SDK examples

TypeScript and Python examples will be added only for operations proven against
the pinned official E2B SDK versions. Examples must destroy sandboxes in cleanup
paths, avoid embedded credentials, and work with loopback-only defaults.
