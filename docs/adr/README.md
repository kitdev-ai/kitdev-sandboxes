# Architecture decision records

ADRs document decisions with broad or durable consequences. Status begins as
`proposed` during Milestone 0 and becomes `accepted`, `rejected`, or
`superseded` through review. A later change adds a new ADR rather than rewriting
the historical rationale.
