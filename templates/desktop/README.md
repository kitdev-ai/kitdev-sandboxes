# Desktop template

Planned E2B Desktop-compatible XFCE/browser/streaming template. Acceptance
covers authenticated streams, screenshots, input/window operations, SDK parity,
credential redaction, and teardown. Host GDM is unrelated and must remain
untouched.
