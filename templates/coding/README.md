# Coding template

Planned language/toolchain template derived from `base`. Tool versions,
licenses, checksums, caches, and update policy must be explicit. Credentials are
never embedded in the artifact.
