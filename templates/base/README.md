# Base template

Planned minimal non-root Ubuntu guest with `envd`, shell/core tools, CA
certificates, Git, and process utilities. Exact release and packages depend on
the upstream compatibility matrix.
